import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 0,
    marginTop: -35,
    padding: 0,
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export { styles };
